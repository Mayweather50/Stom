@model ProjectStomatology.Models.Rclient
@{
    ViewBag.Title = Model.Name;
}
<h2>Пользователь @Model.Name</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Идентификатор</dt>
        <dd>@Model.Id</dd>

        <dt>Имя</dt>
        <dd>@Model.Name</dd>
    </dl>
</div>